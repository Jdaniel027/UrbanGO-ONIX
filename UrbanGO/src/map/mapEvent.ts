/**
 * MapEvents
 * ----------
 * Centraliza los eventos del mapa.
 *
 * 🔹 Maneja interacciones del usuario con el mapa.
 * 🔹 Permite reaccionar a movimientos, zooms o gestos.
 *
 * Responsabilidades:
 * - Escuchar eventos como:
 *   - onPress
 *   - onRegionDidChange
 *   - onCameraChanged
 * - Emitir callbacks o notificaciones al resto de la app
 *
 * Ejemplos:
 * - Detectar cuando el usuario mueve el mapa
 * - Saber cuándo el usuario deja de seguir su ubicación
 *
 * Nota:
 * - No debe renderizar UI
 * - Solo maneja eventos
 */
